<pre>
<span style="color:#aaa;">@@ -900,11 +900,11 @@</span>
         (html
          [:div.ActiveForm {:class (-> route :handler :tag name)}
           (when-not modal?
<span style="color:red;">-            [:div.wrapper.wrapper-content.row.border-bottom.white-bg.page-heading
-             [:h1 (config/page-headers (:tag handler))</span>
<span style="color:green;">+            (row
+             {:class "border-bottom wrapper wrapper-content page-heading white-bg"} [:h1 (config/page-headers (:tag handler))
               (when loading [:i.fa.fa-refresh.fa-spin.pull-right])]</span>
<span style="color:red;">-             (om/build Breadcrumbs nil)])
-          [:div.row.white-bg {:class (when-not modal? "wrapper wrapper-content")}</span>
<span style="color:green;">+             (om/build Breadcrumbs nil)))
+          (row {:class (str "white-bg " (when-not modal? "wrapper wrapper-content"))}</span>
            (om/build FormErrors props)
</pre>
